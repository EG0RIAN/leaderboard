# Команды бота

## /start

Команда `/start` регистрирует пользователя в системе и отправляет приветственное сообщение.

### Использование:

```
/start
```

или с реферальной ссылкой:

```
/start ref_123456789
```

### Что происходит:

1. **Регистрация пользователя:**
   - Если пользователь новый - создается запись в базе данных
   - Если пользователь уже существует - обновляется информация (username, имя и т.д.)

2. **Обработка реферальной ссылки:**
   - Если команда содержит `ref_<id>`, пользователь привязывается к рефереру
   - Реферер закрепляется только для новых пользователей
   - Если пользователь уже существует, реферер не меняется

3. **Приветственное сообщение:**
   - Для новых пользователей: "Добро пожаловать в Лидерборд донатов!"
   - Для существующих: "С возвращением!"
   - Информация о возможностях приложения
   - Призыв открыть Mini App

### Пример ответа:

```
👋 Привет, Иван!

✅ Добро пожаловать в Лидерборд донатов!

📊 Здесь ты можешь:
• Смотреть лидерборды по донатам
• Пополнять свой баланс
• Приглашать друзей и получать бонусы

🎮 Открой Mini App через кнопку меню, чтобы начать!
```

## Реферальные ссылки

Реферальные ссылки можно создавать в формате:
```
https://t.me/your_bot?start=ref_123456789
```

Где `123456789` - это Telegram ID реферера.

## Inline-режим (моё место и реферальная ссылка)

В любом чате пользователь может ввести **@имя_бота** — появится результат с его местом в лидерборде и реферальной ссылкой. При выборе результата в чат отправляется сообщение вида:

```
🏆 Моё место в лидерборде: #123
📊 Чартсов: 456

Присоединяйся по моей ссылке:
https://...
```

**Включение Inline-режима:** в [@BotFather](https://t.me/BotFather) выберите бота → Bot Settings → Inline Mode → Turn on.

## Настройка webhook

Для работы бота через webhook нужно настроить:

```bash
curl -X POST "https://api.telegram.org/bot<BOT_TOKEN>/setWebhook" \
  -d "url=https://your-domain.com/webhook/telegram"
```

Или использовать polling для разработки (не рекомендуется для production).

