# Быстрый старт

## ✅ Выполнено

1. ✅ Создан `.env` файл с настройками
2. ✅ Установлены зависимости (`pip install -r requirements.txt`)
3. ✅ Применены миграции базы данных (`alembic upgrade head`)
4. ✅ Backend запущен и работает на `http://localhost:8000`

## 🔧 Что нужно настроить

### 1. Настроить Telegram бота

1. Создайте бота через [@BotFather](https://t.me/BotFather):
   - Отправьте `/newbot`
   - Следуйте инструкциям
   - Сохраните полученный токен

2. Обновите `.env` файл:
   ```bash
   BOT_TOKEN=ваш_токен_от_BotFather
   TELEGRAM_BOT_USERNAME=username_вашего_бота
   ```

3. Настройте Mini App:
   - В BotFather выберите вашего бота
   - Выберите "Bot Settings" → "Menu Button"
   - Укажите:
     - Текст кнопки: "Лидерборд"
     - URL: `https://your-domain.com/frontend/index.html` (или ваш URL)

### 2. Настроить Webhook для платежей

Для обработки платежей Telegram Stars нужно настроить webhook:

```bash
curl -X POST "https://api.telegram.org/bot<BOT_TOKEN>/setWebhook" \
  -d "url=https://your-domain.com/webhook/telegram"
```

Или используйте метод `setWebhook` через Telegram Bot API.

### 3. Настроить Frontend

1. Обновите `API_BASE_URL` в `frontend/app.js`:
   ```javascript
   const API_BASE_URL = 'https://your-domain.com';
   ```

2. Разместите frontend на веб-сервере (nginx, Apache, или любой статический хостинг)

3. Убедитесь, что frontend доступен по HTTPS (обязательно для Telegram Mini Apps)

## 🚀 Запуск

### Backend (уже запущен)
```bash
python3 run.py
# или
uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000
```

### Frontend (для разработки)
```bash
cd frontend
python3 -m http.server 8001
```

Для production используйте nginx или другой веб-сервер.

## 📝 Проверка работы

1. Проверьте health endpoint:
   ```bash
   curl http://localhost:8000/health
   ```
   Должен вернуть: `{"status":"ok"}`

2. Проверьте API документацию:
   Откройте в браузере: `http://localhost:8000/docs`

## ⚠️ Важно

- Для production обязательно используйте PostgreSQL вместо SQLite
- Настройте правильные CORS origins (только Telegram домены)
- Используйте HTTPS для Mini App
- Храните секреты в переменных окружения, не коммитьте `.env` в git

## 📚 Дополнительная документация

- [SETUP.md](SETUP.md) - подробная инструкция по установке
- [IMPLEMENTATION.md](IMPLEMENTATION.md) - описание реализации
- [README.md](README.md) - общее описание проекта

