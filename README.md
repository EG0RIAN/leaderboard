# Telegram Mini App "Лидерборд донатов + рефералы"

Telegram Mini App для отображения лидербордов по донатам в тонах с реферальной системой.

## Возможности

- 📊 **Три лидерборда**: за всё время, за неделю, топ рефералов
- 💰 **Пополнение донатов**: через Telegram Stars с предустановленными пакетами
- 🔗 **Реферальная система**: приглашение друзей и получение места в лидерборде рефералов
- 💱 **Курс валют**: настраиваемый курс Stars → тоны с кешированием
- 🔒 **Безопасность**: валидация Telegram initData, идемпотентность платежей

## Быстрый старт

1. **Установить зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

2. **Настроить `.env` файл:**
   - Скопировать из `.env.example`
   - Указать `BOT_TOKEN` и `TELEGRAM_BOT_USERNAME`

3. **Применить миграции:**
   ```bash
   alembic upgrade head
   ```

4. **Запустить backend и frontend:**
   ```bash
   # Автоматический запуск обоих сервисов
   ./start.sh
   
   # Или вручную:
   # Backend:
   python3 run.py
   # Frontend (в другом терминале):
   cd frontend && python3 -m http.server 8001
   ```

5. **Остановить сервисы:**
   ```bash
   ./stop.sh
   ```

5. **Настроить Mini App в Telegram:**
   - Создать бота через @BotFather
   - Настроить Menu Button с URL вашего frontend

Подробная инструкция в [SETUP.md](SETUP.md)

## Структура проекта

```
telegram_leaderboard/
├── backend/              # FastAPI backend
│   ├── routers/         # API endpoints
│   ├── services/        # Бизнес-логика
│   ├── models.py        # SQLAlchemy модели
│   └── main.py          # FastAPI приложение
├── frontend/            # Telegram Mini App (SPA)
│   ├── index.html
│   ├── app.js
│   └── styles.css
├── alembic/             # Миграции БД
├── requirements.txt     # Python зависимости
└── run.py               # Точка входа
```

## API Endpoints

- `GET /me` - данные пользователя и статистика
- `GET /leaderboard/all-time` - лидерборд за всё время
- `GET /leaderboard/week` - лидерборд за неделю
- `GET /leaderboard/referrals` - лидерборд рефералов
- `GET /transactions` - история транзакций
- `POST /payments/create-invoice` - создание инвойса
- `POST /webhook/telegram` - webhook для платежей

## Технологии

- **Backend**: FastAPI, SQLAlchemy, Alembic, aiogram
- **Frontend**: Telegram Web Apps SDK, Liquid UI
- **Database**: PostgreSQL / SQLite
- **Payments**: Telegram Stars

## Документация

- [SETUP.md](SETUP.md) - подробная инструкция по установке и настройке
- [IMPLEMENTATION.md](IMPLEMENTATION.md) - описание реализации

## Лицензия

Проект создан в соответствии с техническим заданием.

